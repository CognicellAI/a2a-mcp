name: Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing SemVer tag to validate and publish, for example v0.1.0"
        required: true
        type: string

permissions:
  contents: write
  id-token: write

concurrency:
  group: release-${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
  cancel-in-progress: false

jobs:
  publish:
    name: Publish npm package and GitHub release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}
          fetch-depth: 0

      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"
          package-manager-cache: false

      - name: Ensure trusted-publishing capable npm
        run: npm install -g npm@latest

      - name: Resolve release tag
        id: release
        shell: bash
        run: |
          if [[ "${GITHUB_EVENT_NAME}" == "workflow_dispatch" ]]; then
            tag="${{ inputs.tag }}"
          else
            tag="${GITHUB_REF_NAME}"
          fi

          echo "tag=${tag}" >> "${GITHUB_OUTPUT}"

          version="${tag#v}"
          echo "version=${version}" >> "${GITHUB_OUTPUT}"

          if [[ "${version}" == *-* ]]; then
            echo "npm_tag=next" >> "${GITHUB_OUTPUT}"
            echo "github_release_flags=--prerelease" >> "${GITHUB_OUTPUT}"
          else
            echo "npm_tag=latest" >> "${GITHUB_OUTPUT}"
            echo "github_release_flags=" >> "${GITHUB_OUTPUT}"
          fi

          package_name="$(node -p "require('./package.json').name")"
          echo "package_name=${package_name}" >> "${GITHUB_OUTPUT}"

      - run: npm ci

      - name: Verify SemVer tag matches package version
        run: npm run release:verify-tag -- "${{ steps.release.outputs.tag }}"

      - run: npm run check

      - run: npm audit --omit=dev

      - name: Pack release tarball
        id: pack
        shell: bash
        run: |
          npm pack --json --silent > pack.json
          tarball="$(node -e "const pack = JSON.parse(require('fs').readFileSync('pack.json', 'utf8')); const entry = Array.isArray(pack) ? pack[0] : pack; if (!entry?.filename) throw new Error('npm pack JSON did not include filename'); process.stdout.write(entry.filename)")"
          echo "tarball=${tarball}" >> "${GITHUB_OUTPUT}"

      - name: Check whether npm version already exists
        id: npm_version
        shell: bash
        run: |
          if npm view "${{ steps.release.outputs.package_name }}@${{ steps.release.outputs.version }}" version >/dev/null 2>&1; then
            echo "exists=true" >> "${GITHUB_OUTPUT}"
          else
            echo "exists=false" >> "${GITHUB_OUTPUT}"
          fi

      - name: Publish to npm
        if: steps.npm_version.outputs.exists == 'false'
        run: npm publish "${{ steps.pack.outputs.tarball }}" --access public --tag "${{ steps.release.outputs.npm_tag }}" --provenance

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          gh release create "${{ steps.release.outputs.tag }}" \
            "${{ steps.pack.outputs.tarball }}" \
            --title "${{ steps.release.outputs.tag }}" \
            --generate-notes \
            ${{ steps.release.outputs.github_release_flags }}
